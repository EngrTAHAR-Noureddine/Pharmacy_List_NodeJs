<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.smartpharm">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo_smart_pharm"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_smart_pharm_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartPharm">
        <activity
            android:name=".pharmacist.PharmacistActivity"
            android:exported="true" />
        <activity
            android:name=".client.ClientActivity"
            android:exported="true" />
        <activity
            android:name=".login.LoginActivity"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>